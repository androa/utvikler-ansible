describe package('google-chrome-stable') do
  it { should be_installed }
end

describe package('icaclient') do
  it { should be_installed }
end

describe package('freerdp') do
  it { should be_installed }
end

describe package('hipchat') do
  it { should be_installed }
end

